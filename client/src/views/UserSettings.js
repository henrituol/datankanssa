const UserSettings = () => {
    return(
        <div>
            <h2>User settings</h2>
            <p>To be added after the login system is ready: delete data</p>
        </div>
    )
}

export default UserSettings;