import * as d3 from "d3";

const Histogram = (props) => {
    console.log(props)
    return (
        <div>
            <p>
                {props.data}
            </p>
        </div>
    )
}

export default Histogram;